import React from 'react'

import './styles.scss'

const FlatlistScholarships = ({ children }) => {
  return (
    <section className="flatlist-scholarships">
      {children}
    </section>
  )
}

export default FlatlistScholarships
