import React from 'react'

import './styles.scss'

const ListScholarships = ({ children }) => {
  return (
    <section className="list-scholarships">
      {children}
    </section>
  )
}

export default ListScholarships
